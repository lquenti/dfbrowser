{
  "name": "dfbrowser",
  "private": true,
  "version": "0.0.1",
  "description": "WIP",
  "main": "index.js",
  "scripts": {
    "start": "web-ext run -s src --target firefox-desktop --start-url 'https://www.google.com'",
    "lint": "web-ext lint -s src",
    "build": "web-ext build -s src"
  },
  "author": "lquenti",
  "license": "MIT",
  "devDependencies": {
    "web-ext": "^7.11.0"
  }
}
